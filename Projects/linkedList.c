#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

typedef struct
{
    int vaule;
    LinkedList* next;
}LinkedList;

LinkedList LinkedListAdd(int added)
{
    int value = *list->list[0];
}

int main()
{   //integer linked list

    return 0;
}